# 模板:泛用 Java 工作(新功能 / 重構 / 批次修 bug)

> 在 loop 外的 session 使用,產出 goal.md + plan.json(+選配分析文件)。
> validate cmd:`mvn -q test`(不是 compile——行為等價的守門靠測試,不是靠編譯)。

## 0. Goal(同步產出 goal.md)

`<一句話大目標,刻意粗略>`。DoD:`mvn test` 綠 + 下列行為條目全數有測試證據。

## 1. 行為條目(需求盤點)

| # | 行為(輸入→輸出/副作用) | 依據(檔:行 或 需求原文) | 釘行為的測試 |
|---|---|---|---|
| R1 | | | |

- 重構類:先列「不可變行為」清單——重構的 DoD 是這些測試在改動前後都綠(characterization)。
- 修 bug 類:每個 bug 一列,先寫「重現測試」(紅)再修(綠)。

## 2. 拆件決策規則

`<這個 codebase 的慣例:分層、命名、Mapper 位置、ApiResponse 包裝……執行 agent 照抄不猜>`

## 3. 任務化(loop 規劃期會轉成 plan JSON 並補漏)

- T01: 補測試骨架/重現測試(先紅)
- T02: `<實作/修復單元>`(對應 R#,粒度一輪做得完,DoD 寫明)
- ...

## 4. 完整性 gate

行為條目每列對得到「已完成任務 + 測試(檔:行)」;`mvn test` 綠。
