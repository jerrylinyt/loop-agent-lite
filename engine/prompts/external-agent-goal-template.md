# Goal

## 目標

以「<<TEMPLATE_LABEL>>」為工作類型，將下列初始需求收斂成一份具體、可驗收且不擴張範圍的目標：

<<REQUIREMENT_EXAMPLE>>

- AC-1：把初始需求的主要成果改寫成可觀察的完成狀態。
- AC-2：把需求中的「全部」「完整」「等價」等字眼展開成可逐項核對的 inventory。

## 背景與現況

- 任務類型說明：<<TEMPLATE_DESCRIPTION>>
- Repo 已證實現況：`[填入實際檔案:行號、符號或設定來源]`。
- 需求提供的事實：`[只填原始需求明確陳述的背景]`。
- 推論與待驗證假設：`[分開列出理由及需要補取的證據]`。

## 範圍

- SC-1（來源：AC-1）：`[主要可交付成果與明確邊界]`。
- SC-2（來源：AC-2）：`[inventory 的盤點範圍、來源與排除方式]`。
- SC-3（來源：INV-1）：依此工作類型核對下列分析重點，適用項納入範圍，不適用項附 repo 證據判定 N/A：

<<TEMPLATE_FOCUS>>

## 非目標

- `[不在本次處理的相鄰功能]`；排除依據：`[需求原文或 repo 證據]`。
- 不在缺乏證據時順手重寫、最佳化或更換架構；若原始需求明確要求，改列入對應 SC／AC。

## 限制與相容性

- 沿用 repo 的既有 build、測試、命名、模組與真相來源；偏離處需附需求依據與相容影響。
- 驗證命令、執行目錄、版本與必要前置必須從 repo／CI／官方文件證實，不得保留虛構命令。
- `[補上此工作類型涉及的版本、權限、資料、部署、共存窗口或回退限制]`。

## 完成定義（DoD）

1. DOD-1（驗證 SC-1／AC-1）：在 `[repo 實際執行目錄]` 執行 `[repo 已證實的主要驗證命令]`，exit code 為 0，且輸出符合 `[具體通過判準]`。
2. DOD-2（驗證 SC-2／AC-2）：執行 `[可重跑的 inventory 完整性核對命令或腳本]`，所有 INV／AC 均能追到 SC 及測試證據，缺少任一 ID 即失敗。
3. DOD-3（驗證 SC-3／INV-1）：執行 `[此工作類型的專屬測試、contract、smoke、掃描或文件核對程序]`，通過判準為 `[明確結果]`。

## 人工驗收

1. UAT-1（驗證 `[SC／AC ID]`）：`[只能由人判讀的視覺、UX、產品、權限或上線結果]`。無法命令化的原因：`[說明為何不能由命令／exit code 判定]`。
2. 若所有要求皆可自動驗證，刪除此列並寫「無：所有範圍已有可重跑命令與明確通過判準」。

## 待確認事項

- `[未知資訊]`；影響：`[會阻擋或改變哪些 SC／DoD]`；解除方式：`[由誰提供何種決策或證據]`。
- 若沒有待確認事項，改寫為「無」，並附已檢查的 repo 證據與範圍；不得只寫「未提及」。
